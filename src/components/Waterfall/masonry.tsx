import React, { SFC, Fragment } from 'react'
// import MasonryContext from './MasonryContext'

const Masonry: SFC = ({ children }) => {
  return <Fragment>{children}</Fragment>
}

Masonry.displayName = 'Masonry'

export default Masonry
